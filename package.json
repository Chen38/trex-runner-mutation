{
  "name": "trex-runner-mutation",
  "version": "1.0.0",
  "description": "tRex runner game from Google Chrome offline",
  "main": "index.html",
  "scripts": {
    "start": "gulp dev",
    "build": "rimraf -- dist && gulp build && gulp buildInject",
    "deploy": "git subtree push --prefix=dist origin gh-pages"
  },
  "keywords": [
    "tRex",
    "game",
    "mutation"
  ],
  "author": "Barry Chen",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.24.4",
    "gulp": "^3.9.1",
    "gulp-cleancss": "^0.2.2",
    "gulp-concat": "^2.6.1",
    "gulp-inject": "^4.3.2",
    "gulp-load-plugins": "^1.5.0",
    "gulp-uglify": "^3.0.0",
    "pump": "^3.0.0",
    "rimraf": "^2.6.2",
    "sort-stream": "^1.0.1"
  }
}
